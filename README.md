# Spencer Kingman - Personal Page

## How to run

To run this project, you can use the following command:

```bash
bundle exec jekyll serve
```

This will start a local server and you can access the website at `http://localhost:4000`. You can make changes to the content and see them reflected in real-time.
